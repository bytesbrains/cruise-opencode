# Secrets never enter this repo (Cruise keys, tokens, etc.).
# Keep them in the environment or a local .env — never in git.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

*.log
.DS_Store
node_modules/
dist/
build/
coverage/
.cache/
.DS_Store
*.tgz
