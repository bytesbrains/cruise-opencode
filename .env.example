# Local secrets only — copy to `.env` (gitignored). Never commit real values.
# Prefer the demo host + `cru_demo_` key for rehearsal.
CRUISE_BASE_URL=https://cruise-demo.bytesbrains.net/v1
CRUISE_API_KEY=cru_demo_…
